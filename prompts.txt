1. pick <object> :object={circle, rectangle, triangle}
2. pick the object that looks like a pyramid
3. pick the object that has the color of a leaf
# if arm is not specified, it uses the left arm by default
4. pick the one object using the right arm
5. pick any two objects using the left arm
6. pick all the objects use the left arm more than the right
7. pick the object that doesn't have any vertices
8. don't pick the object that doesn't have a vertex, pick the rest
9. pick the object that looks like the eye of the illuminati
10.pick the closest thing to frisbee
